import React from "react";
const Navbar = () => {
    return (
        <nav>
            <h1>Contact Book</h1>
            {/* Add navigation links here */}
        </nav>
    );
};

export default Navbar;
